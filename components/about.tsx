import React from 'react';
import { useRef, useState, useEffect, ChangeEvent } from 'react';



const About = () => {

    const aboutComponent = useRef<any>(null)
    return (
        <div ref={aboutComponent} className='about'>
            ascdascas
        </div>
    );
};

export default About;